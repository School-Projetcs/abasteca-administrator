import React from 'react';
import { Container } from '@components/styled-system';
import { HeadlineHeader } from './headline-header';

export const Headline: React.FC = () => {
    return (
        <Container>
            <HeadlineHeader />
        </Container>
    );
};
